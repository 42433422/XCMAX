---
description: XCMAX 稳定产品版本 1.0.0.0 与工具链版本 1.0.0 的全产品线强约束
alwaysApply: true
---

# 稳定版本锁定（全产品线）

当前对外稳定产品版本统一为 **`1.0.0.0`**。发布目录、下载清单、Windows 文件版本、Python/FastAPI、Android `versionName` 和 Mod 兼容基线使用四段版本。

npm/Electron、Dart/Flutter pub、iOS/macOS `MARKETING_VERSION` 因工具链限制使用等价三段版本 **`1.0.0`**。这只是格式映射，不是另一个产品版本。

- 新制品不得使用历史 `10.0.0`。
- 禁止只修改单端版本；先改 `FHD/VERSION.md`，再运行版本同步和校验。
- 历史制品、CHANGELOG、证据目录中的旧版本事实不回写。
- 发版前必须通过 `python3 FHD/scripts/dev/verify_version_anchors.py`。

唯一数字 SSOT：[`FHD/VERSION.md`](../../FHD/VERSION.md)。
