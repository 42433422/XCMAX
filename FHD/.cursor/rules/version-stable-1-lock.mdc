---
description: FHD 稳定产品版本 1.0.0.0 与工具链版本 1.0.0 的全栈映射
alwaysApply: true
---

# 稳定版本锁定

对外产品版本固定为 **`1.0.0.0`**；npm/Electron、Flutter pub 和 Apple 市场版本固定映射为 **`1.0.0`**。版本锚点、映射表和变更出口均以 [`VERSION.md`](../../VERSION.md) 为准。

改功能、重构和重新打包不会自动改变版本号。新发布不得复用历史 `10.0.0` 口径；历史证据不得回写。发版前运行：

```bash
python3 scripts/dev/version_sync.py --apply
python3 scripts/dev/verify_version_anchors.py
```
