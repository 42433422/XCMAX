# ============================================================================
# Runtime secrets / local state (NEVER commit — 由 get_app_data_dir() dev 回落
# 写入仓库根，含 token / 本机路径。见 mobile_relay_desktop_client.py。)
# ============================================================================
mobile_relay_desktop.json
wechat_decrypt_runtime.json

# ============================================================================
# Python
# ============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
!desktop/build/
!desktop/build/**
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!scripts/deploy/lib/
!scripts/deploy/lib/**
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/

# ============================================================================
# IDE / editor metadata
# ============================================================================
.idea/
.vscode/
.trae/
.cursor/logs/
*.swp
*.swo
*.bak
*~

# ============================================================================
# Testing & coverage caches (NOTE: do NOT ignore tests/ directory itself)
# ============================================================================
.pytest_cache/
.coverage
htmlcov/
*.cover
coverage.xml
coverage.json
# Point-in-time coverage dumps at FHD root (slipped past bare coverage.json above)
/coverage_*.json
/coverage-*.json
coverage/
# 仓库根的 coverage.py 转储（瞬时产物，曾被误提交 ~38MB）；真正的棘轮基线在 FHD/metrics/ 下，不受此影响。
# Anchored with a leading slash so SSOT baselines under metrics/ stay tracked.
/coverage*.json
# Non-standard frontend coverage output dirs (coverage-target/, coverage-current/, ...)
frontend/coverage-*/
.tox/
.vector_index/

# 运行时端口文件（run_fastapi.py 自动写入，前端 Vite 读取联动）
.runtime/

# ============================================================================
# Database files (runtime data - keep code references to schemas)
# ============================================================================
*.db
*.db-shm
*.db-wal
*.db.backup*
*.sqlite
*.sqlite3
data/dev-db/
XCAGI/data/desktop-dev/
XCAGI/data/enterprise-desktop/
data/model_payment_orders.json
customer_products_final_corrected.db
# Runtime RAG index (dataset_rag_app_service.py writes datasets.json + *.sqlite under get_app_data_dir(); pure runtime data)
/dataset_rag/
# Test-DB toggle state (test_db_manager runtime state; held leaked Windows local paths, not a seed)
XCAGI/.test_db_state.json

# Stray local scratch (historical)
XCAGI/dy
login_out.json
project.private.config.json
**/secret.yaml
!**/secret.yaml.example
scripts/ai_agent_v0/demo.local.env

# ============================================================================
# Logs & temporary output
# ============================================================================
*.log
logs/
.agent-*.log
.uvicorn.*.log
uvicorn-smoke.log
bartender_gui_automation.log
test_ocr_modify.log
test_trademark_files.log
tsc-errors.txt
cleanup_report.txt
**/_fail*.txt
_fail*.txt
_full*.txt
_pytest*.txt
_sweep*.txt
startup_output.txt
test_image.*
test_barcode*

# ============================================================================
# OS
# ============================================================================
.DS_Store
Thumbs.db

# ============================================================================
# Secrets & payment keys
# ============================================================================
.env
.env.*
!.env.example
!.env.*.example
.env.fhd-docker
**/payment_orders/
!**/payment_orders/README.md
**/payment_orders/order_*.json
**/keys_staging/
.env.local
.env.*.local
# Vite build-mode presets (no secrets; safe to track)
!frontend/.env.generic
!frontend/.env.minimal
scripts/package/upload-release.env
.secrets/
*.der
**/chroma/
**/chroma_db/
**/login_out.json
**/alipay_app_private.pem
**/alipay_*_private*.pem
**/应用私钥*.txt
**/应用私钥*.pem
.tmp_alipay_pkg/
# 桌面中继运行时配对状态（mobile_relay_desktop_client.py 自动写入，含 desktop_token/pairing_code，
# 轮换敏感字段；dev/源码运行时 get_app_data_dir() 回落到 FHD 根，故在此忽略避免污染工作树/入库）
/mobile_relay_desktop.json

# ============================================================================
# Runtime output / generated / user data
# ============================================================================
uploads/
temp_excel/
shipment_outputs/
saved_analyses/
user_memory/
labels/
test_labels/
output/
outputs/
temp/

# Excel temp artifacts
~$*.xlsx
~$*.xls

# Label generator outputs
labels/*.png
labels/*.jpg

# Resource exports (customer trademarks etc.)
resources/ai_assistant/*.png
resources/ai_assistant/*.jpg
resources/ai_assistant/*.jpeg
resources/ai_assistant/商标导出/

# Wechat decrypt binary (build artifact)
wechat-decrypt/bin/
wechat-decrypt/build/

# ============================================================================
# Frontend build artifacts
# ============================================================================
node_modules/
frontend/node_modules/
frontend/.nm-e2e/
frontend/.nm-*/
**/.nm-*/
frontend/.vite/
frontend/dist/
templates/vue-dist/
vue-dist/

# ============================================================================
# Model checkpoints (large binaries)
# ============================================================================
*.pt
*.safetensors
checkpoints/
distillation/checkpoints/

# ============================================================================
# Python 32bit distribution
# ============================================================================
python32/

# ============================================================================
# Archive / backups
# ============================================================================
.archive/
git-backup/
release-bundle/
_backup_icon_system/
_tmp_*/
_perf_*.json

# ============================================================================
# Misc
# ============================================================================
git-change-inventory.txt
arch_violations.txt
test_reports/intent_benchmark_latest.json
*.spec
!scripts/package/xcagi_backend.spec
current_page.html
custom_mode_test.html
.accelerate/

# ============================================================================
# Large local data / release builds
# ============================================================================
release/
update-server/
424/
# Sunbird custom installer seed (tracked)
!delivery/
!delivery/sunbird-seed/
!delivery/sunbird-seed/**
ces/
10.2.0
sz-qsm-pro-1.0.0.xcmod
data/exports/
合同/
_compare_convert_output.xlsx

# Removed mods snapshot (restored from git; may contain large zips)
_archived_removed_mods/

# detect_migration_status.py 运行产物(CI artifact,勿提交)
migration_metrics.json
migration_status_report.txt

# mutmut 3.x 变异测试沙箱与缓存（运行时生成约80M，勿提交）
mutants/
.mutmut-cache

# 移动端签名材料与构建产物（CI 由 secret 运行时解码生成，严禁提交）
mobile-harmony/signing/
mobile-harmony/artifacts/
mobile-ios/*.xcodeproj/
mobile-ios/build/
*.p12
*.p7b
*.mobileprovision
*.keystore
*.jks
