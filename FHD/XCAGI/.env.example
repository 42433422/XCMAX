# XCAGI — 复制为与本文件同目录下的 ``.env`` 后按需修改。
# ``run_fastapi.py`` / ``run.py`` 使用 ``load_dotenv(XCAGI/.env)``（本目录），不是仓库根 ``.env``。
# 仓库根 ``app/config.py`` 还会在 import 时加载 ``<repo>/.env``；两处可并存，重复键以先写入 ``os.environ`` 的为准（通常为先启动的 XCAGI/.env）。

# 含 app 包的 FHD 仓库根(``import app.*`` 时用)。XCAGI 与 app 同仓时可不设置(由 ``app/fastapi_app.py`` 等向上探测含 ``app/fastapi_routes/`` 的目录)。
# FHD_REPO_ROOT=E:/FHD

# FHD 壳层 GET /api/mods* 扫描 manifest 用：含 mods/ 子目录的 XCAGI 根路径（本机绝对路径）。
# 若侧栏一直提示「未检测到扩展」，在启动服务前设置其一（xcagi-backend.cmd 已默认设 XCAGI_ROOT）。
# XCAGI_ROOT=E:/FHD/XCAGI
# 或直接指向 mods 目录：
# XCAGI_MODS_DIR=E:/FHD/XCAGI/mods

# 可选：公网 MOD Catalog 根 URL（无尾斜杠）。设置后 /api/mod-store/catalog 会合并 GET {XCAGI_MOD_CATALOG_URL}/v1/index.json。
# XCAGI_MOD_CATALOG_URL=https://mods.example.com

# --- 桌面交付（SQLite，无需本段 Postgres）---
# 使用 start-desktop-sqlite.bat、start-dev.bat 或 run_fastapi.py --desktop。
# 说明见 docs/guides/DESKTOP_DATABASE_DELIVERY.md

# --- Web / 自托管 PostgreSQL 开发 ---
# Required for production: main application database (SQLAlchemy + Alembic).
# Example local Postgres with psycopg v3 driver:
DATABASE_URL=postgresql+psycopg://xcagi:xcagi@localhost:5432/xcagi

# 若业务数据在仓库内历史 SQLite（示例路径，请按本机实际文件修改）:
# DATABASE_URL=sqlite:///E:/FHD/424/products.db

# Optional: vector store (Excel chunks, user memory). Defaults to same URL as DATABASE_URL if unset.
# VECTOR_DB_URL=postgresql+psycopg://xcagi:xcagi@localhost:5432/xcagi

# Optional: purchase_units / 客户应用层数据源。不设则与 DATABASE_URL 相同（已废弃独立 customers.db 运行时依赖）。
# CUSTOMERS_DATABASE_URL=postgresql+psycopg://xcagi:xcagi@localhost:5432/xcagi

# 浏览器 fetch(..., credentials:'include') 时，后端 CORS 不能使用 *，须列出前端源（逗号分隔）。不设则包含 5001/5173 等常用本地端口。
# CORS_ALLOW_ORIGINS=http://127.0.0.1:5001,http://localhost:5001

# Uvicorn：run_fastapi.py 默认开启热重载（改 backend/app 等自动重启）。不需要子进程时可关闭:
# XCAGI_UVICORN_RELOAD=0

# 按请求头 X-XCAGI-Active-Mod-Id 拆业务库：
# - SQLite：无需此项，会自动使用 products__<mod>.db 等（见 app.db）。
# - PostgreSQL：须设 XCAGI_MOD_ISOLATED_DATABASES=1 才会把库名改为 {DATABASE_URL 中的库名}__<mod>，
#   并需先为各扩展建库 + 迁移（见 scripts/bootstrap_mod_postgres_databases.py）；不设则所有 Mod 共用同一 PG 库。
# XCAGI_MOD_ISOLATED_DATABASES=1
# 可选：显式映射某个扩展到自己的连接串（JSON 或每扩展一个环境变量）:
# XCAGI_MOD_DATABASE_URLS={"sz-qsm-pro":"postgresql+psycopg://user:pass@localhost:5432/xcagi__sz_qsm_pro"}
# XCAGI_MOD_DATABASE_URL_SZ_QSM_PRO=postgresql+psycopg://...
# 按 Mod 覆盖 DB 读写令牌（可与设置页当前 Mod 令牌配合）：
# FHD_DB_READ_TOKEN_BY_MODS={"sz-qsm-pro":"read-token-a","example-mod":"read-token-b"}
# FHD_DB_WRITE_TOKEN_BY_MODS={"sz-qsm-pro":"write-token-a","example-mod":"write-token-b"}
# 或使用单项环境变量：
# FHD_DB_READ_TOKEN_SZ_QSM_PRO=read-token-a
# FHD_DB_WRITE_TOKEN_SZ_QSM_PRO=write-token-a
# 为每个扩展在 Postgres 中建库并启用 vector（需先设 DATABASE_URL 指向基库）:
#   python scripts/bootstrap_mod_postgres_databases.py
#   python scripts/migrate_mod_postgres_databases.py

# Postgres must have extension: CREATE EXTENSION IF NOT EXISTS vector;
# New database (repo root):
#   python scripts/bootstrap_postgres.py
#   python -m alembic upgrade head
# Optional — copy legacy SQLite excel_vectors.db into PG:
#   python scripts/migrate_excel_vectors_sqlite_to_pg.py --sqlite path/to/excel_vectors.db

# SECRET_KEY=
# CACHE_REDIS_URL=redis://localhost:6379/0

# --- 模型支付 / 支付宝（与仓库根 .env 二选一或并存；先 load 的键优先生效）---
# 私钥只放环境变量或本机路径，验证通过后删除磁盘上的 .pem。换行用 \n 写入一行：
# ALIPAY_APP_ID=
# ALIPAY_APP_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----
# ALIPAY_APP_PRIVATE_KEY_PATH=
# ALIPAY_NOTIFY_URL=https://你的域名/api/model-payment/notify/alipay
# ALIPAY_DEBUG=1
