{
  "schema_version": 1,
  "kind": "fix",
  "created_at": "2026-08-05T03:56:46+00:00",
  "symptom": "When the self-maintenance governance audit path exists but cannot be read, the policy logs the OSError, treats the audit as empty healthy history, and allows auto-continuation and auto-merge.",
  "root_cause": "_read_governance_audit converted an authorization-relevant read failure into an empty list, while _governance_audit_gate treated every health value except bad as safe and the runtime status duplicated that fail-open decision.",
  "fix_diff": "diff --git a/成都修茈科技有限公司/MODstore_deploy/modstore_server/self_maintenance_loop_runner.py b/成都修茈科技有限公司/MODstore_deploy/modstore_server/self_maintenance_loop_runner.py\nindex 95eb104e1..394a8efb0 100644\n--- a/成都修茈科技有限公司/MODstore_deploy/modstore_server/self_maintenance_loop_runner.py\n+++ b/成都修茈科技有限公司/MODstore_deploy/modstore_server/self_maintenance_loop_runner.py\n@@ -538,7 +538,7 @@ def _read_governance_audit(limit: int = 10) -> List[Dict[str, Any]]:\n                     rows.append(row)\n     except OSError:\n         logger.exception(\"failed to read self-maintenance governance audit\")\n-        return []\n+        raise\n     return rows[-limit:]\n \n \n@@ -586,7 +586,18 @@ def record_governance_audit_review(\n def _governance_audit_summary(\n     rows: Optional[List[Dict[str, Any]]] = None,\n ) -> Dict[str, Any]:\n-    items = rows if isinstance(rows, list) else _read_governance_audit(10)\n+    try:\n+        items = rows if isinstance(rows, list) else _read_governance_audit(10)\n+    except OSError as exc:\n+        return {\n+            \"recent_count\": 0,\n+            \"success_count\": 0,\n+            \"failure_count\": 0,\n+            \"consecutive_failures\": 0,\n+            \"health\": \"unavailable\",\n+            \"error_type\": type(exc).__name__,\n+            \"error\": str(exc)[:300],\n+        }\n     success_count = sum(\n         1 for item in items if isinstance(item, dict) and item.get(\"ok\") is not False\n     )\n@@ -609,12 +620,17 @@ def _governance_audit_summary(\n def _governance_audit_gate() -> Dict[str, Any]:\n     summary = _governance_audit_summary()\n     health = str(summary.get(\"health\") or \"\").strip()\n-    ok = health != \"bad\"\n+    ok = health in {\"ok\", \"warn\"}\n+    reason = {\n+        \"bad\": \"governance_audit_consecutive_failures\",\n+        \"ok\": \"governance_audit_healthy\",\n+        \"warn\": \"governance_audit_healthy\",\n+    }.get(health, \"governance_audit_unavailable\")\n     return {\n         \"ok\": ok,\n         \"blocking\": not ok,\n         \"action\": \"allow\" if ok else \"hold_for_governance_review\",\n-        \"reason\": (\"governance_audit_healthy\" if ok else \"governance_audit_consecutive_failures\"),\n+        \"reason\": reason,\n         \"summary\": summary,\n         \"policy\": \"consecutive_governance_action_failures_pause_auto_continue_and_auto_merge\",\n     }\n@@ -7805,24 +7821,7 @@ def get_self_maintenance_runtime_status(limit: int = 80) -> Dict[str, Any]:\n         \"windows\": metric_windows[-2:],\n     }\n \n-    governance_audit = _read_governance_audit(10)\n-    governance_audit_summary = _governance_audit_summary(governance_audit)\n-    governance_gate_current = {\n-        \"ok\": governance_audit_summary.get(\"health\") != \"bad\",\n-        \"blocking\": governance_audit_summary.get(\"health\") == \"bad\",\n-        \"action\": (\n-            \"hold_for_governance_review\"\n-            if governance_audit_summary.get(\"health\") == \"bad\"\n-            else \"allow\"\n-        ),\n-        \"reason\": (\n-            \"governance_audit_consecutive_failures\"\n-            if governance_audit_summary.get(\"health\") == \"bad\"\n-            else \"governance_audit_healthy\"\n-        ),\n-        \"summary\": governance_audit_summary,\n-        \"policy\": \"consecutive_governance_action_failures_pause_auto_continue_and_auto_merge\",\n-    }\n+    governance_gate_current = _governance_audit_gate()\n     roster_gate_current = (\n         roster_alignment.get(\"gate\") if isinstance(roster_alignment.get(\"gate\"), dict) else {}\n     )\ndiff --git a/成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py b/成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py\nindex e9000a285..98869b741 100644\n--- a/成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py\n+++ b/成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py\n@@ -81,6 +81,55 @@ QUALITY_CHECKS_JSON = (\n )\n \n \n+def test_governance_audit_gate_fails_closed_when_existing_audit_is_unreadable(\n+    monkeypatch,\n+) -> None:\n+    class UnreadableAuditPath:\n+        @staticmethod\n+        def exists() -> bool:\n+            return True\n+\n+        @staticmethod\n+        def open(*_args, **_kwargs):\n+            raise OSError(\"permission denied\")\n+\n+    monkeypatch.setattr(loop_runner, \"governance_audit_path\", UnreadableAuditPath)\n+\n+    with pytest.raises(OSError, match=\"permission denied\"):\n+        loop_runner._read_governance_audit()\n+\n+    gate = loop_runner._governance_audit_gate()\n+\n+    assert gate[\"ok\"] is False\n+    assert gate[\"blocking\"] is True\n+    assert gate[\"action\"] == \"hold_for_governance_review\"\n+    assert gate[\"reason\"] == \"governance_audit_unavailable\"\n+    assert gate[\"summary\"] == {\n+        \"recent_count\": 0,\n+        \"success_count\": 0,\n+        \"failure_count\": 0,\n+        \"consecutive_failures\": 0,\n+        \"health\": \"unavailable\",\n+        \"error_type\": \"OSError\",\n+        \"error\": \"permission denied\",\n+    }\n+\n+\n+def test_governance_audit_gate_allows_missing_audit_history(monkeypatch, tmp_path) -> None:\n+    monkeypatch.setenv(\n+        \"MODSTORE_SELF_MAINTENANCE_GOVERNANCE_AUDIT\",\n+        str(tmp_path / \"missing-governance-audit.jsonl\"),\n+    )\n+\n+    gate = loop_runner._governance_audit_gate()\n+\n+    assert gate[\"ok\"] is True\n+    assert gate[\"blocking\"] is False\n+    assert gate[\"action\"] == \"allow\"\n+    assert gate[\"reason\"] == \"governance_audit_healthy\"\n+    assert gate[\"summary\"][\"health\"] == \"ok\"\n+\n+\n def test_scheduler_selected_remediation_is_resolved_exactly() -> None:\n     memory = {\n         \"open_items\": [\n",
  "metadata": {
    "component": "self_maintenance_governance_gate",
    "files": [
      "成都修茈科技有限公司/MODstore_deploy/modstore_server/self_maintenance_loop_runner.py",
      "成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py"
    ]
  },
  "executable_template": {
    "applicability_check": "The governance audit path exists and opening or reading it raises OSError; confirm the current gate reports governance_audit_healthy or otherwise permits automatic continuation.",
    "patch_strategy": "Propagate governance-audit OSError, translate it into an unavailable health summary, allow only explicit ok or warn health values, and reuse the same gate result in runtime status; retain missing-file behavior and add focused regressions for both boundaries.",
    "rollback_plan": "Revert the reader, summary, gate, status deduplication, and focused tests together; until another authoritative audit-read signal is available, disable unattended continuation and merge when the audit cannot be read.",
    "required_tests": [
      "成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py::test_governance_audit_gate_fails_closed_when_existing_audit_is_unreadable",
      "成都修茈科技有限公司/MODstore_deploy/tests/test_self_maintenance_loop_runner_policy.py::test_governance_audit_gate_allows_missing_audit_history"
    ]
  }
}
