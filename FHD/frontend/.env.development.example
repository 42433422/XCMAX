# 复制为 frontend/.env.development（git 忽略）
#
# 端口 SSOT（与 scripts/dev/start-enterprise-dev.sh 一致）：
#   企业 desktop API :5000  + 企业 Vite :5001
#   管理 web API     :42422 + 管理 Vite :5011/admin/
# 并排第二套树才用 :5100/:5101（见 .env.development.local.example 模式 B）。
#
# 开发环境：不要设置 VITE_API_BASE_URL（或保持为空）
# 这样 /api/* 使用相对路径，由 vite 代理到本机后端，与页面同源，
# 避免跨域、以及后端冷启动/热重载时浏览器直连失败 / ERR_ABORTED。
#
# 仅在需要浏览器直连后端排查问题时再取消注释：
# VITE_API_BASE_URL=http://127.0.0.1:5000

# 与后端 FHD_DB_WRITE_TOKEN 一致（本地开发；生产请改强随机串且勿提交）
VITE_DB_WRITE_TOKEN=change-me-to-match-backend
