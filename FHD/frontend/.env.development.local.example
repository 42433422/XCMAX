# 复制为 .env.development.local（git 忽略），只保留一种模式，勿混用端口。
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 模式 A — start-enterprise-dev.sh（推荐）                                 │
# │   企业 API :5000  企业 Vite :5001                                        │
# │   管理 API :42422 管理 Vite :5011（admin-console 由脚本设 VITE_*，勿写错）│
# └─────────────────────────────────────────────────────────────────────────┘
VITE_XCAGI_PRODUCT_SKU=enterprise
VITE_DEV_PORT=5001
VITE_API_BASE=http://127.0.0.1:5000
VITE_PERSONNEL_MOD_ID=xcmax-personnel
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ 模式 B — 并排第二套树（FHD-个人 等）                                       │
# │   后端 :5100  前端 :5101                                                 │
# └─────────────────────────────────────────────────────────────────────────┘
# VITE_XCAGI_PRODUCT_SKU=enterprise
# VITE_DEV_PORT=5101
# VITE_API_BASE=http://127.0.0.1:5100
#
# 勿将 VITE_API_BASE 设为 5003：旧文档笔误。
# 勿把管理端 Vite 指到 desktop :5000（会 ADMIN_DESKTOP_FORBIDDEN）。
# 单独跑管理端时：
#   cd admin-console && VITE_DEV_PORT=5011 VITE_API_BASE=http://127.0.0.1:42422 npm run dev
#
# 本机 Clash/系统代理：localhost 必须 bypass，否则浏览器访问 :5001/:5011 → 502。
#   source FHD/scripts/dev/ensure_dev_proxy_bypass.sh
#   或用 FHD/scripts/launchers/start-cursor-with-proxy.sh 启动 Cursor
