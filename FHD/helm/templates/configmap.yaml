apiVersion: v1
kind: ConfigMap
metadata:
  name: xcagi-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "xcagi-fhd-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
