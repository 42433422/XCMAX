{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(argo_rollouts_rollout_info{name=\"xcagi\"}) by (phase)",
          "legendFormat": "{{phase}}"
        }
      ],
      "title": "Rollout phase (xcagi)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(argo_rollouts_rollout_reconcile_bucket[5m])) by (le))",
          "legendFormat": "reconcile p95"
        }
      ],
      "title": "Rollout controller reconcile latency",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["xcagi", "rollouts"],
  "title": "XCAGI Rollouts Canary",
  "uid": "xcagi-rollouts",
  "version": 1
}
