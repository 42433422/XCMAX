# DEPRECATED: use secret.yaml.example + kubectl create secret / SealedSecrets.
# This file is intentionally NOT included in kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: xcagi-secrets
type: Opaque
stringData:
  deepseek_api_key: "REPLACE_ME_USE_SEALED_SECRETS"
  database_url: "REPLACE_ME_USE_SEALED_SECRETS"
