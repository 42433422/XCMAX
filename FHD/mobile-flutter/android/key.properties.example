# 复制为 key.properties 或 keystore.properties（已 gitignore），勿提交仓库。
# 也可用环境变量 XCAGI_ANDROID_KEYSTORE*（优先级更高）。
# 正式发版可设 XCAGI_REQUIRE_RELEASE_SIGNING=1，缺密钥时失败而非回退 debug。

storeFile=signing/xcagi-release.jks
storePassword=你的密钥库密码
keyAlias=xcagi_release
keyPassword=你的密钥密码
