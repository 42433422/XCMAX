# Grafana 仪表盘预置 — 自动加载 dashboards/ 下的 JSON
apiVersion: 1

providers:
  - name: 'xcagi-fhd'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards