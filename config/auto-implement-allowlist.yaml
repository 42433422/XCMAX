# ai-issue-implement 域预授权（方案 B）
#
# 规则：
#   1. issue 仍须带 `ai-implement` 标签才会触发 workflow
#   2. 若 issue 另有 label 全量匹配下列任一 regex，则跳过 owner「确认」评论门禁
#   3. 未命中 allowlist 时，维持现状（须 owner 评论确认）
#
# 风险：仅白名单域自动执行；改此文件即改预授权面，PR 需人工审。
#
# ^incident$ 由 cvm-autonomy-watcher 创建的 incident issue 自动预授权：
#   - incident issue 由 watcher 兜底创建（前置 remediation action 失败时）
#   - 标签含 ai-implement + incident + auto-incident + auto-generated
#   - 命中 ^incident$ 跳过 owner 确认，ai-issue-implement.yml workflow 自动生成修复 PR
#   - allowlist 只预授权执行；模型生成的修复标 `ai-generated` + `risk:r1`，
#     经 SLA 二次守卫（CI 全绿/体量/文件类型/禁止路径）+ 48h 观察期 + `hold-merge` veto 后自动合并

version: 1
enabled: true

label_patterns:
  - "^bug-mech-.*"
  - "^bug-fix$"
  - "^refactor-.*"
  - "^tech-debt-.*"
  - "^incident$"

# trusted_authors: ai-self-heal-auto-merge.yml 普通 PR 三重门禁的 author allowlist
# 见 FHD/scripts/ci/ai_self_heal_sla.py:load_trusted_authors
# 普通 PR（无 ai-self-heal / ai-generated label）须 author ∈ trusted_authors 才允许 auto-merge
trusted_authors:
  - "42433422"
  - "github-actions[XCAGI]"
