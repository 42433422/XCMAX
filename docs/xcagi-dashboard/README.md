# XCAGI 全景 Dashboard 静态资源

供根目录 `XCAGI-Full-Pipeline.html`、`XCAGI-Five-Line.html` 引用。

| 文件 | 用途 |
|------|------|
| `dashboard.css` | 设计系统与布局 |
| `app.js` | Router / Ops / Tree / Gaps |
| `tree-worker.js` | 目录树 Worker（由 `app.js` 加载） |

本地打开 HTML 时请从 XCMAX 仓库根目录起服务，或保持相对路径 `docs/xcagi-dashboard/`。
