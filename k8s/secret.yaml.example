# Example only — do NOT apply with placeholder values.
# Use SealedSecrets, ExternalSecrets, or: kubectl create secret generic xcagi-secrets ...
apiVersion: v1
kind: Secret
metadata:
  name: xcagi-secrets
type: Opaque
stringData:
  deepseek_api_key: "<set-via-sealed-secrets-or-ci>"
  database_url: "<set-via-sealed-secrets-or-ci>"
