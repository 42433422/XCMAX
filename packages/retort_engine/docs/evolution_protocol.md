# Retort Evolution Protocol

Retort defaults to isolated assessment and stops only when every score is
strictly greater than 90.

External absorption must load a GitHub or local project, compare it against the
selected main project folder, classify absorbable value, generate employee
tasks on an absorption branch, and merge back only after the branch is clean.
