# 生产运行时（SSOT：pyproject.toml → requirements-base.txt）
# 开发/测试：pip install -r requirements-dev.txt  或  pip install -e ".[dev]"
# ML：pip install -r requirements-ml.txt  或  pip install -e ".[ml]"
# 桌面音频/ASR（Windows phone_agent）：pip install -e ".[desktop]"
#
# 启动：cd XCAGI && python run.py
-r requirements-base.txt
