# XCMAX 全量改进计划（Plan）— 2026-06

> 范围：`/Users/a4243342/Desktop/XCMAX`（含 `FHD/`、`成都修茈科技有限公司/`、`WXCC/`、`specs/` 等）
> 关联规范：[`spec.md`](spec.md) · [旧 `tasks.md`](tasks.md)
> **状态：2026-06 执行轮次已闭环（见 checklist）** — P0 与可文档化脚手架已交付；staging / 空仓 / 业务数据 / DB 收口见 [`BLOCKERS.md`](BLOCKERS.md)
> **执行跟踪（打勾源）**：[`plan-2026-06-checklist.md`](plan-2026-06-checklist.md)
> 原则：**不删任何功能**，只清理工程卫生 / 补真实证据 / 收口技术债
>
> **数字口径 SSOT**：本表「目标」列为 M0–M1 验收权威来源。路径图 [`对标头部SaaS-步骤路径图.md`](对标头部SaaS-步骤路径图.md) 曾写仓根 ≤15 GB / mypy 20→12，已统一为 **≤8 GB**、**ignore_errors 18→6**（2026-06-05）。
>
> **M0 体积口径**[^m0-volume]：**尽调可见体量** = 交付树（**不含 `.git`**）≤ **8 GB** 为 M0 验收 SSOT；本机 `du -sh .` 若含 `FHD/.git`（及本地 `.venv*` 等），**可能 >8 GB**，不等于未达标。2026-W24 基线：`du` **~9.7 GB**（含 `FHD/.git`），交付树剔除 `.git` 后仍对照 ≤8 GB。

---

## 0. 总体目标

| 指标 | 现状（2026-06-04 实测） | 目标 |
|------|--------------------------|------|
| 工作区体积（尽调可见 / 交付树）[^m0-volume] | 47 GB（含历史/产物）；W24 `du` ~9.7 GB（含 `FHD/.git`） | ≤ 8 GB（交付树不含 `.git`；清仓根产物 + 拆仓后） |
| 仓根运行残留 | coverage / dist / cov 等 | 0（`.gitignore` + `git rm --cached`） |
| 路由 `_v2` 文件数 | 24+ | 单调递减；CI 守门 |
| e2e 用例 | 骨架 + CI | 5 条关键链路稳定 + 截图 |
| mypy `ignore_errors` | 18 目录 | ≤ 6（6 个月分批） |
| 真实运营数据截图 | 0 | ≥ 1（staging Grafana） |
| 跨平台桌面 | Windows 为主 | macOS CI 冒烟 |

**不删功能前提下**，按 P0 → P1 → P2 → P3 执行；细节与 68 项 TODO 见 [`plan-2026-06-checklist.md`](plan-2026-06-checklist.md)。

---

## 2026-06 轮次结论

| 类别 | 说明 |
|------|------|
| **已闭环** | P0 全项；P1 文档/e2e/DB 策略/DORA；P2 macOS/Android/DORA；本轮回文档脚手架（SLO runbook、AI 证据模板、拆仓里程碑、Pro-mode 评审、FHD-个人迁出清单） |
| **仍阻塞** | T36–T37（staging 截图）、T56（业务月数据）、T58–T60（GitHub 空仓 + split）、T68（业务签字）、T27–T28（DB 引用链）、mypy T19–T25 |

---

## 关联文档

- 阻塞：[`BLOCKERS.md`](BLOCKERS.md)
- SLO：[`../FHD/docs/SLO.md`](../FHD/docs/SLO.md)
- 可观测性：[`../FHD/docs/observability/README.md`](../FHD/docs/observability/README.md)
- AI 证据：[`../FHD/docs/AI_BUSINESS_EVIDENCE.md`](../FHD/docs/AI_BUSINESS_EVIDENCE.md)
- 拆仓：[`../成都修茈科技有限公司/docs/migration/SPLIT_MILESTONES.md`](../成都修茈科技有限公司/docs/migration/SPLIT_MILESTONES.md)

---

*最后更新：2026-06-05 — M0 体积双口径脚注；每周清单见 [`weekly/`](weekly/)*

[^m0-volume]: **尽调可见体量** = 向客户/投资人展示的交付树（**不含 `.git`**）≤ **8 GB**，为 M0 验收 SSOT。本机 `du -sh .` 若口径含 `FHD/.git` 及本地 `.venv*` 等，**可能 >8 GB**（2026-W24 对齐 **~9.7 GB**），仅作卫生跟踪，**不等于**尽调未达标；复测交付树可用 `du -sh --exclude='.git' .` 或剔除 `FHD/.git` 后对照。详见路径图 [^repo-size]。

