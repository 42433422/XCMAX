# 宿主入门与导航恢复

用户已确认的首次使用流程是「公司名称 → 行业 → 配置」，采用公司蓝色，并按九大行业提供能进入真实功能的侧栏。演示数据与 AI 第一单属于可选体验，不是完成入门的必经步骤。

## 遗漏证据

- `f89118af109d2a195092bfa642b589c4695187bb` 固化了公司名称、行业目录和行业导航；`79a4f758ced4cf445bb012e0dff0f1d286624d0e` 修复选择行业后侧栏不刷新的问题。
- 当时安装候选为 `f6ec3486849fde21607c8e11088109d753bf71f5`。这些对象在本地可读，但均不是原工作区 `91330fadc8ae000496a36508de5743cb4195464f`、本轮候选 `252005df7d5c91e4d13ca1ab7146155bb6bc70c2` 或当时主线 `de4a4755259865851f7b9def13f29b1e2a62a4ca` 的祖先。
- 三个当前版本均缺少历史的 `onboardingIndustryCatalog.ts`、`industryNavigationProfiles.ts` 和公司名输入。因此不是用户记错，也不能把当前五步流程当作已验收设计。
- `252005df7` 的原生客户端中，智能对话实际路由为 `/mod/xcagi-planner-bridge/chat`，侧栏排在多项业务入口之后。原置顶方法只识别字面量 `chat`，遗漏了 Planner Mod 菜单标识。

## 恢复要求与验收

1. 首次登录进入公司名称、行业、配置三步。匿名用户可以先填名称和选择行业；生成配置方案前必须登录，并取得公司名称及行业的真实保存结果。失败时允许重试，不推进到配置或写虚假完成标记。
2. 行业分类支持九大类及细分选择，分类与专版授权分别判断。不能因选择行业而自动授予权限，未就绪功能不能显示为已可用。
3. 完成配置后进入真实业务入口。准备演示数据、排队 AI 任务和写入业务记录需要用户另行选择与必要确认；保留考勤定制交付边界。
4. 智能对话在宿主和 Planner Mod 两种菜单形态下均置顶；旧缓存、拖拽预览、落位及重新加载不能将其移到下方。其他可排序入口保留用户顺序，管理端沿用自身置顶规则。
5. 以新提交的完整候选包复验首次登录、保存/失败恢复、行业切换即时与重登后的导航、置顶和可选演示。历史 `252005df7` 的身份接口通过不能充当新流程通过；源码测试也不能代替原生验收。

本恢复保留 CLI 迁移、登录安全、考勤分流、导入撤销、标签打印和打包身份修复，不整包回退到历史分支。恢复完成与否以对应提交、测试及新候选的实际界面证据记录，90 分目标仍需正式交付和真实客户验证。
