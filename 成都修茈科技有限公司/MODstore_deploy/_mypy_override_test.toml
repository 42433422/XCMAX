[tool.mypy]
python_version = "3.10"
explicit_package_bases = true
mypy_path = ["."]
follow_imports = "skip"

[[tool.mypy.overrides]]
module = ["modstore_server.*"]
ignore_errors = true

[[tool.mypy.overrides]]
module = ["modstore_server.db.catalog"]
ignore_errors = false
