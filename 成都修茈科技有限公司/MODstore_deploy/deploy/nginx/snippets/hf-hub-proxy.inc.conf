# Whisper 浏览器 ASR：静态模型（须 ^~ 优先于 /market/ SPA）
# 目录：/data/hf-hub/onnx-community/whisper-base/resolve/main/config.json
location ^~ /market/hf-hub/ {
    alias /data/hf-hub/;
    autoindex off;
    add_header Cache-Control "public, max-age=86400";
    add_header Access-Control-Allow-Origin "*";
}
