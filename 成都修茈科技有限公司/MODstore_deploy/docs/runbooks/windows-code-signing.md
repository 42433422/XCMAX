# 已迁移：不要从 MODstore 发布桌面稳定版

MODstore `desktop-shell` 仅保留历史兼容用途，个人版已冻结，也不得再向 stable 路径上传制品。

Windows 企业桌面的唯一签名与发布 SSOT：

```text
FHD/docs/runbooks/windows-code-signing.md
```

唯一发布工作流：

```text
.github/workflows/fhd-release-desktop.yml
```
