# 官网根目录 main.js / styles.css（与 /market/ 无关）
location ~ ^/(styles\.css|main\.js)$ {
    root /root/成都修茈科技有限公司;
    try_files $uri =404;
    add_header Cache-Control "no-cache, must-revalidate" always;
}
