[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xcagi-common"
version = "0.1.0"
description = "Shared auth/csrf/session utilities for FHD and MODstore"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["xcagi_common*"]
